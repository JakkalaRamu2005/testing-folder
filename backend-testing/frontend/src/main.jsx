import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'

import App from './App.jsx'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
/* 

don't copy past the code at any point in time, this is very very important, 
as user's of the ai increases, the skill of debugging to the humans is going to decrease
the onething that i have realised is that writing the code is very easy and biulding the 
projects is also very easy but debugging the existing 

*/